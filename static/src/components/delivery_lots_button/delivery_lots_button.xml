<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_picking_form_delivery_lots" model="ir.ui.view">
        <field name="name">stock.picking.form.delivery.lots</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_form"/>
        <field name="arch" type="xml">

            <!--
                IMPORTANTE: En Odoo 19, la lista de movimientos en el picking form
                usa move_ids con una list interna. El xpath debe apuntar al field
                product_id DENTRO de esa list.
                
                Si el xpath falla, probar con:
                  //field[@name='move_ids']//field[@name='product_id']
                o buscar el nombre exacto de la columna en la vista base de Odoo.
            -->
            <xpath expr="//field[@name='move_ids']/list/field[@name='product_id']" position="before">
                <field
                    name="is_dlots_expanded"
                    widget="delivery_lots_button"
                    string=" "
                    nolabel="1"
                    class="p-0 text-center"
                    width="40px"
                />
            </xpath>

        </field>
    </record>
</odoo>